{
  "name": "@ledgermem/anthropic",
  "version": "0.1.0",
  "description": "LedgerMem helper for the Anthropic SDK — memory tool, prompt caching, and extended thinking wired together.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "src", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "keywords": [
    "ledgermem",
    "anthropic",
    "claude",
    "memory",
    "tool-use",
    "prompt-caching"
  ],
  "author": "LedgerMem <founders@proofly.dev>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ledgermem/ledgermem-anthropic.git"
  },
  "dependencies": {
    "@ledgermem/memory": "^0.1.0"
  },
  "peerDependencies": {
    "@anthropic-ai/sdk": ">=0.30.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
